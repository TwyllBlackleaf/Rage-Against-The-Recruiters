<header>
    <nav>
        <% if (req.session.loggedIn) { %>
            <a href="/userPage"><%= req.session.username %></a>
            <button id="logoutButton">Log Out</button>
        <% } else { %>
            <a href="/login">Log In</a>
        <% } %>
    </nav>
</header>